import { DetectedInfo, Header } from "@/components"

const DetectedPage = () => {
    return (
        <div>
            <Header />
            <div className="container">
                <DetectedInfo />
            </div>
        </div>
    )
}

export default DetectedPage